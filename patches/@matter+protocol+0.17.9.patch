diff --git a/node_modules/@matter/protocol/dist/cjs/action/client/subscription/ClientSubscriptionHandler.js b/node_modules/@matter/protocol/dist/cjs/action/client/subscription/ClientSubscriptionHandler.js
index e42e0df..1da8f91 100644
--- a/node_modules/@matter/protocol/dist/cjs/action/client/subscription/ClientSubscriptionHandler.js
+++ b/node_modules/@matter/protocol/dist/cjs/action/client/subscription/ClientSubscriptionHandler.js
@@ -116,7 +116,27 @@ class ClientSubscriptionHandler {
   async #handleExchange(exchange) {
     const messenger = new import_InteractionMessenger.IncomingInteractionClientMessenger(exchange);
     const abort = this.#subscriptions.readingAbortSignal;
-    const reports = messenger.readDataReports({ abort });
+    // Fork patch (ACK-failure fast resubscribe): when our final success ACK for a
+    // data report cannot be delivered, the peer session is effectively dead and any
+    // follow-up report (e.g. a door close) is lost until resubscribe. Close the
+    // subscription immediately so SustainedSubscription replaces it now instead of
+    // at the natural subscription timeout (~11 min for 10-min-interval devices).
+    let ackFailureSubscription;
+    const reports = messenger.readDataReports({
+      abort,
+      onFinalAckFailure: error => {
+        if (ackFailureSubscription === void 0 || abort.aborted) {
+          return;
+        }
+        logger.info(
+          exchange.via,
+          "Peer unresponsive acking data report; replacing subscription",
+          import_general.Diagnostic.strong(import_Subscription.Subscription.idStrOf(ackFailureSubscription.subscriptionId)),
+          import_general.Diagnostic.errorMessage(error)
+        );
+        ackFailureSubscription.close();
+      }
+    });
     const initialIteration = await reports.next();
     if (initialIteration.done) {
       throw new import_general.InternalError("Exchange initiated with no initial message");
@@ -148,6 +168,7 @@ class ClientSubscriptionHandler {
       }
       return;
     }
+    ackFailureSubscription = subscription;
     try {
       subscription.isReading = true;
       subscription.lastReportStartedAt = import_general.Time.nowMs;
diff --git a/node_modules/@matter/protocol/dist/cjs/interaction/InteractionMessenger.js b/node_modules/@matter/protocol/dist/cjs/interaction/InteractionMessenger.js
index ca335c0..b1ab38b 100644
--- a/node_modules/@matter/protocol/dist/cjs/interaction/InteractionMessenger.js
+++ b/node_modules/@matter/protocol/dist/cjs/interaction/InteractionMessenger.js
@@ -658,6 +658,16 @@ class IncomingInteractionClientMessenger extends InteractionMessenger {
             this.sendStatus(import_types.Status.Success, {
               multipleMessageInteraction: true,
               logContext: this.#logContextOf(report)
+            }).catch(error => {
+              // Fork patch (ACK-failure fast resubscribe): surface the failure to the
+              // caller (ClientSubscriptionHandler) so a dead subscription can be
+              // replaced immediately instead of waiting for its natural timeout.
+              try {
+                options?.onFinalAckFailure?.(error);
+              } catch (handlerError) {
+                logger.warn(this.exchange.via, "Error in onFinalAckFailure handler", handlerError);
+              }
+              throw error;
             }),
             "Error sending success after final data report chunk"
           );
diff --git a/node_modules/@matter/protocol/dist/esm/action/client/subscription/ClientSubscriptionHandler.js b/node_modules/@matter/protocol/dist/esm/action/client/subscription/ClientSubscriptionHandler.js
index 0619013..ad88d07 100644
--- a/node_modules/@matter/protocol/dist/esm/action/client/subscription/ClientSubscriptionHandler.js
+++ b/node_modules/@matter/protocol/dist/esm/action/client/subscription/ClientSubscriptionHandler.js
@@ -93,7 +93,27 @@ class ClientSubscriptionHandler {
   async #handleExchange(exchange) {
     const messenger = new IncomingInteractionClientMessenger(exchange);
     const abort = this.#subscriptions.readingAbortSignal;
-    const reports = messenger.readDataReports({ abort });
+    // Fork patch (ACK-failure fast resubscribe): when our final success ACK for a
+    // data report cannot be delivered, the peer session is effectively dead and any
+    // follow-up report (e.g. a door close) is lost until resubscribe. Close the
+    // subscription immediately so SustainedSubscription replaces it now instead of
+    // at the natural subscription timeout (~11 min for 10-min-interval devices).
+    let ackFailureSubscription;
+    const reports = messenger.readDataReports({
+      abort,
+      onFinalAckFailure: error => {
+        if (ackFailureSubscription === void 0 || abort.aborted) {
+          return;
+        }
+        logger.info(
+          exchange.via,
+          "Peer unresponsive acking data report; replacing subscription",
+          Diagnostic.strong(Subscription.idStrOf(ackFailureSubscription.subscriptionId)),
+          Diagnostic.errorMessage(error)
+        );
+        ackFailureSubscription.close();
+      }
+    });
     const initialIteration = await reports.next();
     if (initialIteration.done) {
       throw new InternalError("Exchange initiated with no initial message");
@@ -125,6 +145,7 @@ class ClientSubscriptionHandler {
       }
       return;
     }
+    ackFailureSubscription = subscription;
     try {
       subscription.isReading = true;
       subscription.lastReportStartedAt = Time.nowMs;
diff --git a/node_modules/@matter/protocol/dist/esm/interaction/InteractionMessenger.js b/node_modules/@matter/protocol/dist/esm/interaction/InteractionMessenger.js
index 4c3c505..d85b69a 100644
--- a/node_modules/@matter/protocol/dist/esm/interaction/InteractionMessenger.js
+++ b/node_modules/@matter/protocol/dist/esm/interaction/InteractionMessenger.js
@@ -664,6 +664,16 @@ class IncomingInteractionClientMessenger extends InteractionMessenger {
             this.sendStatus(Status.Success, {
               multipleMessageInteraction: true,
               logContext: this.#logContextOf(report)
+            }).catch(error => {
+              // Fork patch (ACK-failure fast resubscribe): surface the failure to the
+              // caller (ClientSubscriptionHandler) so a dead subscription can be
+              // replaced immediately instead of waiting for its natural timeout.
+              try {
+                options?.onFinalAckFailure?.(error);
+              } catch (handlerError) {
+                logger.warn(this.exchange.via, "Error in onFinalAckFailure handler", handlerError);
+              }
+              throw error;
             }),
             "Error sending success after final data report chunk"
           );
